#!/bin/bash

function installed {
    if [ -z "$(fc-list | grep "Fira Code")" ]; then
        return 1
    fi
    return 0
} 

function install {
    sudo pacman -S --noconfirm ttf-fira-code
}

function uninstall {
    sudo pacman -R --noconfirm ttf-fira-code
}

function setup {
    exit 0
}
