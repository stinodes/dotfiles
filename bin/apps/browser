#!/bin/bash

function installed {
    stn-is-installed zen
}

function install {
    yay -S --noconfirm zen-browser-bin
}

function uninstall {
    yay -R --noconfirm zen-browser-bin
}

function setup {
    exit 0
}
