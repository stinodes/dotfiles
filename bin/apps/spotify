#!/bin/bash

function installed {
    stn-is-installed spotify_player
}

function install {
    sudo pacman -S --noconfirm spotify-player
}

function uninstall {
    sudo pacman -R --noconfirm spotify-player
}

function setup {
    exit 0
}
