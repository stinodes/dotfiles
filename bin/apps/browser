#!/bin/bash

function installed {
    if stn-is-installed zen || stn-is-installed zen-browser; then
        exit 0
    fi
    exit 1

}

function install {
    yay -S --noconfirm zen-browser-bin
}

function uninstall {
    yay -R --noconfirm zen-browser-bin
}

function setup {
    exit 0
}
