#!/bin/bash

function installed {
    stn-is-installed spotify_player
    stn-is-installed bluetui
    stn-is-installed impala
    stn-is-installed btm
    stn-is-installed lazygit
    stn-is-installed lazydocker
    stn-is-installed wiremix
    stn-is-installed neofetch
}

function install {
    cargo install spotify_player --features pixelate,notify
    sudo pacman -S --noconfirm bluetui impala bottom lazygit lazydocker wiremix neofetch
}

function uninstall {
    sudo pacman -R --noconfirm bluetui impala bottom lazygit lazydocker wiremix neofetch
    cargo uninstall spotify_player
}

function setup {
    ln -nsf $STN_CONFIG_DIR/neofetch $HOME/.config/neofetch
}
