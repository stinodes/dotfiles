#!/bin/bash

function installed {
    if stn-is-installed brave; then
        exit 0
    fi
    exit 1
}

function install {
    yay -S --noconfirm brave-bin
}

function uninstall {
    yay -R --noconfirm brave-bin
}

function setup {
    exit 0
}
