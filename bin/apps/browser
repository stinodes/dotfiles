#!/bin/bash

function installed {
    if stn-is-installed brave-origin; then
        exit 0
    fi
    exit 1
}

function install {
    yay -S --noconfirm brave-origin-bin
}

function uninstall {
    yay -R --noconfirm brave-origin-bin
}

function setup {
    exit 0
}
